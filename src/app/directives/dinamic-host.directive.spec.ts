import { DinamicHostDirective } from './dinamic-host.directive';

describe('DinamicHostDirective', () => {
  it('should create an instance', () => {
    const directive = new DinamicHostDirective();
    expect(directive).toBeTruthy();
  });
});
